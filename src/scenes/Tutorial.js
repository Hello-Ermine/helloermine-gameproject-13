import Phaser from "phaser";

class Tutorial extends Phaser.Scene {
    constructor(test) {
        super({
            key: 'Tutorial'
        });
    }

    preload() {
        
    }

    create() {
        
    }

    update(delta, time) {
        
    }
}
export default Tutorial;
