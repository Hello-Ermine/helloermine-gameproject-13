import Phaser from "phaser";

class Credit extends Phaser.Scene {
    constructor(test) {
        super({
            key: 'Credit'
        });
    }

    preload() {
        
    }

    create() {
        
    }

    update(delta, time) {
        
    }
}
export default Credit;
