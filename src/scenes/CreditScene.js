import Phaser from "phaser";

class CreditScene extends Phaser.Scene {
    constructor(test) {
        super({
            key: 'CreditScene'
        });
    }

    preload() {
        
    }

    create() {
        
    }

    update(delta, time) {
        
    }
}
export default CreditScene;
